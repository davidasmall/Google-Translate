# Google-Translate
Essentially google translate within the terminal using user inputs.
User can input anything into the terminal,
Will then be prompted to choose a language(s) to translate it into to,
Option to either translate into another language or translate something else will appear upon completion.
